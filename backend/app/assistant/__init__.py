"""Launchpad architect assistant (SE-039): a member conversation with the protected
``aws-agent-solution-architect`` preset that ends in an inert, reviewable proposal
for ONE new managed-Harness business agent, executed only by a separate explicit
approval through the normal deploy pipeline."""

# Correction pass 2 for SE-005 — AWS ClientErrors on console routes map to 4xx envelopes, never a bare 500 or raw boto text

The host re-ran the acceptance checks on your branch and found the following. Fix exactly this, re-run `make verify`, commit on the current branch, and report as before. Do not start unrelated work.

Host review: every acceptance check passes on my own rerun (make verify PASS; live curl from a throwaway :8012 backend on your branch: registry → 400 aws.validation, KB → 403 aws.access_denied, memory actor → 404 aws.not_found; console screenshots read correctly). One correction on the public-API boundary before I open the PR: the /v1 routes (backend/app/routers/public_api.py — invoke and invoke-stream reach AWS) share this handler, so an API-key consumer would now receive, e.g., 'User: arn:aws:sts::<account-id>:assumed-role/admin_role_for_workshop/<instance-id> is not authorized to perform: bedrock:…' in a 403 body, plus detail.operation — before this change they got an opaque 500. That leaks the deployment's role ARN and instance id across the API-key trust boundary. Fix in client_error_handler: when request.url.path starts with '/v1', keep the mapped status and code but use a generic message per code (e.g. 'AWS resource not found' / 'AWS rejected the request as invalid' / 'AWS access denied' / 'AWS is throttling this request' / 'AWS resource conflict') and set detail to {'aws_error_code': <code>} only (no operation, no raw message). Console /api routes keep the full stripped message as now. Add a hermetic test in backend/tests/test_errors_aws.py that mounts a /v1/... and an /api/... route raising the same AccessDeniedException ClientError and asserts the /v1 body contains neither the raw message text nor 'operation' while the /api body does. Update the one sentence in docs/api.md (+ zh-CN) that says /v1 and /api share the envelopes to state the /v1 message is generic. Run everything in the FOREGROUND, run make verify, commit on the branch, leave the tree clean, and end with the five-section report including git log --oneline main..HEAD.
